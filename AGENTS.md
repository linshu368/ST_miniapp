# ST_miniapp

Telegram Mini App（AI 角色扮演），与 Bot 共享 Supabase 数据库。

## 新会话 Bootstrap 协议（AI 必读）

AI 在每次新会话开始时，**必须**按以下顺序完成 bootstrap，再接受 PM 的任务。
协议本身不硬编码任何子目录或具体文件名，项目如何膨胀都不影响本协议的正确性。

### Step 1 — 发现并读完所有规则文档

1. 读本文件（项目根的 `AGENTS.md`）。
2. 读本文件「必读文档索引」里链接的**每一篇**文档，全文阅读。
3. 在整个 workspace 中搜索所有 `AGENTS.md`（根目录及任意子目录，递归），**全部读完**。
   - 规则：**就近优先** — 当你在某个子目录（package / 模块）里工作时，离你最近的 `AGENTS.md` 优先级最高；根 `AGENTS.md` 提供全局约定。
   - 子目录 `AGENTS.md` 与根冲突时，先指出冲突再等 PM 决策。
4. 读 workspace 根的 `package.json` 与 workspace 配置，确认包管理器、脚本约定、engines 要求。
5. 对你本次任务将要接触到的 package，读它的 `package.json` 以了解已装依赖。

### Step 2a — 结构扫描（必做，成本极低）

**只看目录树和文件名，不读文件内容。** 这一步的目的是建立「骨架地图」，方便 Step 2b 精准定位。

1. 列出 workspace 根目录、以及 `packages/` 下每个 package 根目录的一级文件和子目录。
2. 对每个 package，列出其源码目录（通常是 `src/`）的**递归**目录树（到文件名级别即可，不读内容）。
3. 如发现任何 `AGENTS.md` 里描述的结构与现场不符，判定为「文档过期」，在 Step 3 的回复中指出。

> 为什么不读内容：项目会膨胀到几百上千文件，一次性精读既耗 context 又往往无用。真正需要的内容放到 Step 2b 按任务驱动精读，保证协议在任何项目规模下都成立。

### Step 2b — 按需精读（任务驱动，收到 PM 任务后进行）

收到 PM 的具体任务描述后，再读**任务相关**模块的文件内容：

- 任务描述直接涉及的模块（比如要改对话页 → 读对话相关的路由、组件、hook）；
- 这些模块依赖的共享封装（`packages/shared/` 里的类型、数据层对应文件、与其交互的 stores）；
- 任务描述里不涉及的模块**不读**。

**复用优先**：如果 Step 2a 的地图里已经存在明显与任务重叠的封装（例如任务要写"角色选择 hook"，而地图里已有 `useCharactersQuery`），**主动指出并复用，不重写**。

### 数据契约纪律（涉及前后端数据类型时必守）

1. 前后端共享的数据形状以 `packages/shared/` 为**唯一真相源**。任何一方新增对外数据形状，都必须先落进 `shared/` 才能被消费——此约束**对前端、后端、AI 同等适用**，详见 `docs/frontend-stack.md` §5.3。
2. AI 在前端新增或扩展数据类型前，只做一件事：**读 `packages/shared/src/`**。
   - 已有 → 直接消费，不重复定义。
   - 没有 → 视为"新领域"，**先在 `shared/` 里定义契约**，再在 `frontend/lib/mock-data/` 写 mock，再写消费代码。
3. **严禁**在前端私自定义与 shared 并行的类型；**严禁**为了推测数据形状去读 `packages/backend/` 源码（只读 shared 就足够）。
4. 风险兜底：如果 shared 的契约本身与后端实现不一致（契约分叉），这属于**前后端协作层面的偏差**，不归前端 AI 独立修复。该偏差将由 PM 与开发共同校准后更新 `shared/`。接到校准后的 shared 继续推进即可。

### Step 3 — Step 2a 完成后按以下模板回复 PM，然后等任务指令

```
✅ Bootstrap 完成（Step 1 + Step 2a）

项目理解：<2-3 句复述项目定位、技术栈来源、PM 工作流>

规则文档清单：<列出你实际读到的所有 AGENTS.md 和必读文档路径>

骨架地图：<Step 2a 的结果：列出关键 package 与其源码目录的树状结构，到文件名级别；不含内容解读>

我已记住的硬红线：<列 5 条最关键的，证明你确实读了规则文档>

⚠️ 我发现的问题/不一致（如有，否则写「无」）：<文档与骨架冲突 / 依赖缺失 / 文档过期描述 等>

等待你的任务指令。收到任务后我会按 Step 2b 精读相关模块再动手。
```

### 协议约束

- Step 1 + Step 2a 完成并按 Step 3 回复前，**不要**写代码、不要猜任务、不要主动建议引入新库。
- Step 2b 的精读是任务驱动的，**收到 PM 任务后再做**；切忌在 Bootstrap 首轮就把全部源码读一遍。
- 文件读不到或路径不存在时，**直接告诉 PM**，不要编造。
- 所有硬规则以现行的 `AGENTS.md` 集合与它们链接的文档为**唯一真相源**；PM 口头指令与文档冲突时，先指出冲突再等 PM 决策。
- 本协议不随项目结构与规模变化而失效。一旦发现本协议本身有漏洞（例如"就近优先"无法决断、文档链式引用断开等），提出来由 PM 更新本文件。

## 必读文档索引

> 本节是 Bootstrap Step 1 的入口。新增规则文档时追加到这里即可，协议自动发现。

- **[前端技术栈约定](./docs/frontend-stack.md)** — 不可谈判的前端选型、代码规范、与 AI 协作约定

## 分支命名（PM 开发流程）

```
pm-xxx/feature-name-YYYYMMDD     ← PM 功能分支
pm-jason/feature-name-YYYYMMDD   ← Jason 作为 PM 身份时
hotfix/xxx                       ← 线上紧急修复
main                             ← 线上生产，受保护
```

每个功能起新分支，合并后分支使命结束。
