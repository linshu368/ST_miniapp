# 裂变邀请 · 阶段二实施计划（双端 UI 与联调）

> 上游文档：`docs/裂变工程落地实施方案.md`（v2）；前置：`docs/裂变阶段一实施计划.md`（✅ 已验收，105 已在 test 库生效）。
>
> 本阶段产物：C 端邀请中心与三处入口、深链捕获与绑定上报、admin 三个 config 编辑器与邀请数据查询页、1 个新迁移（admin 查询 RPC）。**本文档经确认后开始编码。**
>
> 更新时间：0831
>
> **状态：✅ 阶段二全部完成（0831）。** 任务一~七逐节见各自完成记录：106 已对 test 库执行；
> C 端（本窗口）与 admin 侧（并行窗口）编码完毕、门禁全绿；C 端全链路已在本地 + test 库
> E2E 联调通过，四条路由 `@frontend-ready: true`。test 库入口开关现为 `true`（联调态），
> 生产保持 `false`。下一阶段：阶段三（UAT 边界用例、防刷并发重放、三端真机冒烟），
> 机测部分已完成，见 `docs/裂变阶段三实施计划.md`。

---

## 0. 阶段二新增设计决策（需确认）

| #   | 决策                                                                  | 说明                                                                                                                                                                                                                                                                          |
| --- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | 邀请中心路由 `/profile/invite`                                        | 对齐"我的"子页惯例（`(main)/profile/{messages,orders,recharge,…}`），返回键回 `/profile`；充值页与星尘不足弹窗的入口同样跳这里。~~加入 `HIDDEN_PREFIXES`~~ **编码期修正（0831）：** 按 UI 参考图邀请中心底部导航可见，不加入 `HIDDEN_PREFIXES`，页面底部 padding 避让悬浮导航 |
| E2  | `inv_` 参数与渠道归因互斥：`GrowthEntryReporter` 显式跳过 `inv_` 前缀 | 现状：任何非支付前缀的 start_param 都会被当渠道 `source_id` 上报 `/api/growth/miniapp-entry`。不加跳过的话 `inv_XXXX` 会被当成渠道码走 botlinks 逻辑。邀请流量的 `source_id='invite'` 由绑定接口守卫式写入（阶段一 D1），不走 growth 链路                                     |
| E3  | 钻石图标用 Lucide `Gem`                                               | 仓库无钻石图标资源；`Gem` 是 Lucide 现成的钻石形图标，与既有 `Sparkles`（星尘）同库同风格                                                                                                                                                                                     |
| E4  | admin"邀请数据"查询走新 `admin.*` SECURITY DEFINER RPC（迁移 106）    | 探索确认：invite 三表 RLS + 只授 `service_role`，admin 前端经 `/api/admin/supabase` 代理持 anon+JWT 直查必被拒。对齐 outreach / announcements 先例：新建 `admin.list_invite_records`，权限模式复制既有 admin RPC                                                              |
| E5  | 三个 config 的 admin 编辑器用专用表单，不用 raw JSON textarea         | admin 无通用 JSON 编辑器先例，全部是专用表单组件；`miniapp_invite_entry_enabled` 用 Switch（对齐 payment_prompt 的 `enabled` 字段先例）                                                                                                                                       |
| E6  | 被邀请人侧绑定为无感知静默上报                                        | PRD 未要求被邀请人可见反馈；绑定结果只写 log，失败/终态由 sessionStorage 重试策略兜底                                                                                                                                                                                         |

---

## 1. 任务拆分与顺序

```
任务一  迁移 106：admin.list_invite_records RPC（可与任务二并行）
任务二  C 端：深链捕获 + 绑定上报（InviteBindReporter）
任务三  C 端：lib/api/invite.ts + 邀请中心页（邀请 Tab / 数据中心 Tab）
任务四  C 端：三处入口（我的页卡片 / 充值页快捷入口 / 星尘不足弹窗次级按钮）
任务五  B 端：三个 config key 接入 managed config 发布体系
任务六  B 端：裂变邀请管理视图（邀请数据查询）
任务七  联调收尾：test 库真机链路走通后，backend 四条路由 @frontend-ready 切 true
```

任务二～四（PM 侧 C 端）与任务五～六（admin 侧）可并行；任务七收口。

---

## 2. 任务一：迁移 `106_invite_admin_query.sql` ✅ 已完成（0831）

> 已对 **test 库**执行生效（本地 psql 直连，沿用仓库既有先例——文件尚未入 git，GHA 无法引用；
> 生产环境仍按计划走 GHA，与 105 一起在阶段四执行）。
> 执行前做过整体回滚式 dry-run：未鉴权拒绝（42501）、明细含双侧昵称与奖励 JSON、
> granted/none 筛选、UUID/tg_id 双匹配、查无此人空集、非法状态报错（22023）、
> 分页 total_count、绑定时间窗，全部通过；实现中把 ref 解析拆为内部工具函数
> `admin.resolve_invite_user_ref`（不 GRANT 给客户端角色）。

- 位置 `packages/shared/migrations/`，头部声明 `-- domain: acquisition`；经 GHA 对 test 执行（生产与 105 一起推迟到阶段四）。
- 新建 `admin.list_invite_records(p_inviter_ref TEXT, p_invitee_ref TEXT, p_bound_from TIMESTAMPTZ, p_bound_to TIMESTAMPTZ, p_reward_status TEXT, p_limit INT, p_offset INT)`：
  - 明细行 = `invite_relations` 左联 `invite_reward_logs` 聚合 + 双侧 `app_core.users` 的 `tg_id`；
  - 返回列：`inviter_tg_id / inviter_user_id / invitee_tg_id / invitee_user_id / invite_code / bound_at / reward_credits_total / reward_entries(jsonb: rule_key, credits, granted_at) / total_count`；
  - `p_inviter_ref` / `p_invitee_ref` 同时匹配 uuid 或 tg_id（对齐 outreach lookup 的 identifier 习惯）；`p_reward_status` 取 `granted`（已有到账）/ `none`（零到账）/ NULL（不限）；
  - 按 `bound_at DESC`，默认 `limit 50`；**只做明细查询，不做汇总看板**（PRD 明确不做）；
  - 权限与鉴权断言完全复制既有 admin RPC（如 `list_announcements` / `lookup_user_for_credit_grant`）的模式：SECURITY DEFINER + 内部校验 admin 用户与环境权限 + `REVOKE PUBLIC/anon`、GRANT 对齐先例；
  - 迁移尾部 DO 块自检：RPC 存在、对 anon REVOKE 生效。

## 3. 任务二：C 端深链捕获与绑定上报 ✅ 已完成（0831）

> `GrowthEntryReporter` 已加 `inv_` 跳过分支；`InviteBindReporter` 落地：
> `invite_bind_pending`（码缓存，网络失败保留重试）+ `invite_bind_done`（终态去重），
> 五种终态均清 pending 不再重复请求。frontend lint / typecheck / 56 单测全绿。

改动集中在 `packages/frontend/src/app/providers.tsx` + `src/lib/api/invite.ts`（新建，见任务三）：

1. `GrowthEntryReporter` 开头加一条跳过：`startParam.startsWith(INVITE_START_PARAM_PREFIX) → return`（E2，常量从 `@miniapp/shared` 导入）。
2. 新增 `InviteBindReporter`（Providers 内并列挂载，复制 GrowthEntryReporter 模式）：
   - `getStartParam()` 读到 `inv_` 前缀 → 剥前缀得 code → 存 `sessionStorage['invite_bind_pending']`；
   - 有 pending code 时 POST `/api/invite/bind`；返回任意终态（`bound / already_bound / self_invite / not_new_user`）→ 清 pending；`invalid_code` 也清（码不存在，重试无意义）；网络失败 → 保留 pending，下次挂载重试（与 growth 相同的"失败清除标记以便重试"语义，方向相反：成功才清）；
   - 已上报去重：终态后写 `sessionStorage['invite_bind_done']`，避免同会话重复请求。

## 4. 任务三：C 端 API 封装与邀请中心页 ✅ 已完成（0831）

> `lib/api/invite.ts`（React Query：entry-status / center / stats + bindInvite 纯函数；
> center-view 为幂等 POST 按查询建模，first_visit 时本地翻转 entry-status 缓存让提醒标签立即消失）；
> `(main)/profile/invite/page.tsx`：下划线双 Tab，邀请 Tab 含 9:16 海报（空串降级）、
> 可编辑 textarea、模板轮换刷新（纯本地）、同步栈复制 + 1.5s 行内反馈、链接缺失提示；
> 数据中心 Tab 含双统计卡、实时状态点、最近到账列表（rule_key 中文映射）与更新时间。
> 入口开关关闭时页面展示"活动暂未开启"且不触发 center-view 副作用。视觉对齐 UI 参考图。

**`src/lib/api/invite.ts`（新建）**：React Query，对齐 `notifications.ts` / `payment.ts` 模式——`inviteKeys` + 私有 fetch + `useInviteEntryStatusQuery`（GET entry-status）、`useInviteCenterMutation`（POST center-view，进页时调）、`useInviteStatsQuery`（GET stats）、`bindInvite`（纯函数，供 Reporter 用）。类型全部来自 `@miniapp/shared`。

**`src/app/(main)/profile/invite/page.tsx`（新建）**，顶部两个 Tab（复用 messages 页下划线 tablist 先例），默认"邀请"：

- **邀请 Tab：**
  - 进页调 center-view 拿 `invite_code / invite_link / poster_url / copy_templates / first_visit`；
  - 海报：`poster_url` 非空时按 9:16（2160×3840）比例容器展示，空串降级隐藏（PRD 允许运营未发布）；
  - 可编辑文本框（textarea）：初值 = `copy_templates[0]` 的 `{link}` 替换为 `invite_link`；用户可任意改；
  - 刷新按钮：在已发布 `copy_templates` 中**轮换**（index+1 取模），重新做 `{link}` 替换并**覆盖**当前手动修改，链接不变——纯本地操作，不发请求；
  - "复制并分享"：`navigator.clipboard.writeText(当前 textarea 全文)`，**必须在点击同步栈内执行**（数据进页时已在本地，无异步插入）；反馈复用 orders 页行内"已复制"+1.5s 自消先例；
  - `invite_link` 为空串（后端无 bot token 降级）时复制内容不含链接占位，展示提示文案。
- **数据中心 Tab：**
  - `useInviteStatsQuery`：累计邀请人数、累计获得星尘、最近到账记录列表（rule_key 映射中文文案 + credits + 时间）、底部"数据更新时间 HH:mm（实时）"；`update_mode='batch'` 时展示"批量更新"字样（本期恒 realtime，字段已占位）。

## 5. 任务四：C 端三处入口 ✅ 已完成（0831）

> 我的页（余额卡下方，Gem 图标 + 玫瑰渐变"2200星尘"标签 + "去邀请"）、充值页
> （套餐与安全说明之间，玫瑰渐变卡）、星尘不足弹窗（主次双按钮，次级关弹窗直跳）
> 三处均由 `useInviteEntryStatusQuery().entry_enabled` 控制显隐（同 key 共享缓存）。

三处显隐都由 `useInviteEntryStatusQuery` 的 `entry_enabled` 控制（React Query 同 key 共享缓存），`false` 时整体隐藏：

| 位置         | 文件                               | 改动                                                                                                                                                                                                                                                                                               |
| ------------ | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 我的页       | `(main)/profile/page.tsx`          | 星尘余额卡（≈L359–388）与 ProfileRow 列表（≈L390–411）之间插"邀请中心"卡片：`Gem` 图标 + 标题"邀请中心" + 小字"分享专属链接，邀请好友得2200星尘"；`center_entered=false` 时标题右侧挂粉色渐变"2200星尘"chip（复用 plan-card 的 `from-rose to-rose-fill` 渐变），首次进入后由服务端字段翻转自然消失 |
| 充值页       | `(main)/profile/recharge/page.tsx` | 套餐列表（cards 段）与支付安全说明（trust 段）之间插快捷入口："邀请好友得 2200 星尘"+ 说明"分享专属链接，好友首次登录后建立邀请关系"，点击直接 `router.push('/profile/invite')`，无中间确认页                                                                                                      |
| 星尘不足弹窗 | 同上文件 Dialog（≈L329–362）       | DialogFooter 改主次双按钮：保留"选择套餐"主按钮；新增次级"邀请好友得星尘"→ 关闭弹窗并跳 `/profile/invite`                                                                                                                                                                                          |

## 6. 任务五：B 端 config 接入（`packages/admin`）✅ 已完成（0831）

> 三处登记与三个编辑分支均已落地：`configSchemas.ts` 追加三 key（zod 口径与 105 的
> `admin.validate_invite_reward_rules` / `validate_invite_center_config` 一致，defaultValue 与 seed 逐字一致）；
> 新建 `InviteRewardRulesEditor.tsx`（rule_key 只读、无改名/删除入口）与 `InviteCenterConfigEditor.tsx`
> （文案库增删改、删除按钮在仅剩 1 条时禁用、缺 `{link}` 占位符时行内警示）；
> `miniapp_invite_entry_enabled` 为 ConfigValueEditor 行内 Switch。
> 实现细节：编辑器取值用宽松结构归一化（对齐 `asWordCountTiersConfig` 先例），
> 编辑中间态（清空文案等）不会被打回默认值，取值合法性由保存/发布时 zod + DB 校验兜底；全部存 JSON value，不用 text_value。
> admin typecheck + 单测（44 例）全绿。

DB 侧白名单与校验函数 105 已全部就绪，本任务纯前端三处登记 + 编辑器：

1. `lib/configSchemas.ts`：`managedConfigKeys` 追加三 key；zod schema（reward_rules：cap 正整数 + rules 数组 rule_key/credits/enabled；center_config：poster_url string + copy_templates 非空 string 数组；entry_enabled：boolean）；`configMetadata` 补 label/description/defaultValue（与 105 seed 一致）。
2. `components/ConfigValueEditor.tsx` 加三个分支：
   - `miniapp_invite_reward_rules` → 新建 `InviteRewardRulesEditor.tsx`：cap 数字输入 + 规则行列表（rule_key 只读展示 + credits 数字 + enabled Switch）；**rule_key 不提供改名/删除入口**（发布后作幂等键），新增规则行可后置；
   - `miniapp_invite_center_config` → 新建 `InviteCenterConfigEditor.tsx`：poster_url 文本输入（先贴 URL，上传后置）+ 文案库多行列表（增删改，至少 1 条，提示 `{link}` 占位符用法）；
   - `miniapp_invite_entry_enabled` → 行内 `Switch`（对齐 payment_prompt `enabled` 先例）。
3. 保存草稿 / 发布 / 历史回滚全部白拿既有流程，无需改 `App.tsx` 发布逻辑；侧栏 config 菜单由 `managedConfigKeys.map` 自动生成。

## 7. 任务六：B 端裂变邀请管理视图 ✅ 已完成（0831）

> `AdminViewKey` 加 `invite_program`（含 `adminNavigation.test.ts` 用例）；新建
> `components/InviteProgramView.tsx` 与 `lib/inviteAdminApi.ts`（`client.schema('admin').rpc('list_invite_records', …)`，
> 对齐 outreachCreditsApi 模式，无任何 `.from('invite_*')` 直查）；`App.tsx` 已挂渲染分支。
> 实现细节：菜单项挂在侧栏「运营配置」组内（对齐 UI 参考图与「回访星尘赠送」先例，仍是独立 view）；
> 两 Tab 命名对齐参考图为「素材配置 / 邀请数据」；素材配置 Tab 为三张跳转说明卡（切到对应 config 编辑视图）；
> 邀请数据 Tab 四条件筛选 + 服务端分页（limit/offset + total_count，页大小 20–200）+ 行展开奖励明细；
> 绑定时间为闭开区间，结束日 +1 天保证整天覆盖；奖励状态映射：有到账=「已到账」，零到账=「未发放」（本期实时发放，无「待批量更新」态）。
> 新增 `dayjs` 为 admin 直接依赖（RangePicker 取值所需，antd v6 本就基于 dayjs）。

按"独立顶栏入口"先例（对齐公告/角色卡挂法）：

1. `lib/adminNavigation.ts`：`AdminViewKey` 加 `invite_program`，菜单项"裂变邀请管理"；补 `adminNavigation.test.ts` 用例。
2. `components/InviteProgramView.tsx`（新建）：两个 Tab——
   - **素材与规则 Tab**：不重复造编辑器，放三个 config 的跳转说明卡（点击切到对应 config 编辑视图），避免两处编辑同一 key；
   - **邀请数据 Tab**：筛选表单（邀请人 / 被邀请用户 / 绑定时间范围 / 奖励状态）+ Ant Design Table 服务端分页（`admin.list_invite_records` 的 limit/offset + total_count），行内展开显示奖励明细（rule_key/credits/granted_at）。
3. `lib/inviteAdminApi.ts`（新建）：`client.schema('admin').rpc('list_invite_records', …)`，对齐 `outreachCreditsApi.ts` 模式。
4. `App.tsx`：侧栏 items + `view === 'invite_program'` 渲染分支。

## 8. 任务七：联调收尾（backend）✅ 已完成（0831）

> 本地 backend（连 test 库）+ frontend + 浏览器完成 C 端全链路 E2E：
> 我的页入口卡与"2200星尘"标签 → 邀请中心取码（bot 短名经 getMe 实链）→ 手动改文案/刷新覆盖 →
> 新用户带 `inv_` 参数进入 → `bound` 落库（关系 + 200 发奖 + 流水 + `source_id='invite'`）→
> 标签消失（服务端字段）→ 数据中心 1 人/200/到账记录逐项对齐 → 充值页快捷入口与
> 星尘不足弹窗次级按钮直达邀请中心。`source_id='invite'`（而非 `inv_` 原始码）同时反证
> growth 跳过分支生效。QA 用户数据已清理零残留；test 库入口开关保持 `true`。
> 四条路由 `@frontend-ready` 已切 `true`，backend typecheck 通过。
> 遗留：剪贴板复制反馈在内嵌浏览器因标签页非聚焦无法触发（writeText 被浏览器拒），
> 属环境限制，与生产在用的订单复制同模式，真机验证归入阶段三三端冒烟
> （已排入 `docs/裂变阶段三实施计划.md` §6.1 第 7 项）。

- test 环境全链路走通（见 §9 验收）后，把 `routes/invite.ts` 四条路由的 `@frontend-ready: false` 切 `true`（backend CLAUDE.md 清单第 2 条，同 PR 完成）；
- 联调期间在 admin 把 test 环境 `miniapp_invite_entry_enabled` 发布为 `true`（生产保持 `false` 到阶段四）；
- backend 本阶段无新路由；admin 查询不经 backend 新增 API（走 supabase 代理 + admin RPC）。

---

## 9. 验收清单（对齐 PRD"怎么算做完"，阶段二口径）

1. "我的"页余额卡下方出现邀请中心入口（Gem 图标、暖黑金棕视觉）；未进入过时显示粉色渐变"2200星尘"标签，首次进入后多设备一致消失（服务端字段，清缓存不复现）。
2. 邀请中心默认"邀请"Tab：海报按 2160×3840 比例展示（未发布时降级）；文本框可编辑；刷新在已发布文案库中轮换并覆盖手动修改、链接不变；"复制并分享"一次复制全文并给轻反馈。
3. 新设备走 `t.me/{bot}/app?startapp=inv_{code}` 首次登录 → 绑定成功，`invite_relations` 落行、注册奖到账、`source_id='invite'`；老账户 / 自邀 / 乱码 / 重复打开分别命中四个终态，不新增不覆盖。
4. 数据中心数字与 test 库逐条对齐（人数 = relations count、星尘 = reward_logs sum、到账记录时间倒序），展示更新时间与"实时"状态。
5. 充值页快捷入口与星尘不足弹窗次级按钮均能直达邀请中心（弹窗先关闭再跳转）。
6. admin（test 环境）：三个 config 可编辑、保存草稿、发布、历史回滚，非法值被 105 的 DB 校验拒绝并有可读报错；"裂变邀请管理 → 邀请数据"可按四个条件筛出明细，分页正确；生产环境入口开关保持 `false` 时 C 端三处入口全部隐藏。
7. `GrowthEntryReporter` 回归：普通渠道码归因不受影响；`inv_` 参数不再出现在 growth 上报中。
8. 门禁：frontend lint + typecheck、admin typecheck + 单测、backend/shared typecheck 全绿；四条路由 `@frontend-ready: true`。

---

## 10. 主要风险点（编码时的硬约束）

- **剪贴板同步栈：** 复制按钮 onClick 里第一行就是 `writeText`，前面不得插任何 `await`（数据进页已在本地）。
- **`inv_` 与渠道归因互相踩踏：** E2 的跳过分支必须与 InviteBindReporter 同 PR 落地，且补充回归验证（用一个真实渠道码确认 growth 上报仍正常）。
- **admin 直查被拒：** 邀请数据一律走 `admin.list_invite_records`，任何 `.from('invite_*')` 直查在 admin 侧都是错误路径。
- **rule_key 不可变：** admin 编辑器层面锁死 rule_key 改名，防止运营误操作破坏发奖幂等键。
- **PM/Dev 边界：** 任务二～四涉及 `packages/frontend` 的页面与视觉，属 PM 管辖范围；本次按你的指示由同一会话完成，PR 描述中注明。

---

## 11. 需要你确认的事项

| #   | 事项                                                                         | 建议                                                                               |
| --- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 1   | E1 路由 `/profile/invite`（而非顶级 `/invite`）                              | 建议采纳：与"我的"子页返回链路一致；深链场景用户由 startapp 参数进入，不依赖短路径 |
| 2   | E3 钻石图标用 Lucide `Gem`                                                   | 建议采纳；如设计侧后续出专属 SVG 可平替                                            |
| 3   | 106 查询 RPC 的筛选口径（§2：uuid/tg_id 双匹配 + granted/none 两档奖励状态） | 建议采纳；PRD 只要求明细可查                                                       |
| 4   | admin"裂变邀请管理"做独立顶栏入口，素材/规则编辑复用 config 视图不重复造     | 建议采纳，避免同一 key 两处编辑                                                    |
| 5   | 联调用海报素材（2160×3840 图 URL）                                           | 需运营提供；缺省时 poster_url 留空串走降级路径，不阻塞开发                         |

确认后按任务一 → （二～四 ∥ 五～六） → 七顺序编码。
