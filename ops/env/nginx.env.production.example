# =============================================================================
# nginx（Railway 服务 `nginx`）生产环境变量模板 — 方案 Y
# -----------------------------------------------------------------------------
# ops/nginx/nginx.conf 是 envsubst 模板，upstream 用占位变量；容器启动时由 nginx
# 官方 entrypoint（20-envsubst-on-templates.sh）替换为下面两个值。
# 取值 = Railway 内网服务名:端口（<service>.railway.internal:<port>，须与
# .railway/railway.ts 中的服务名一致）。在 Railway 控制台该服务 Variables 配置。
# =============================================================================

# ⚠️ 本模板对应 production 环境（nginx 服务在 prod 实际命名为 `nginx-pro`）。
#    ST 上游服务在 prod 命名为 `st-bundle-pro`（dev 才是 `st-bundle`）；backend 两环境同名 `stminiapp`。
#    另需设 PORT=80（Railway 据此把公网域名路由到容器，缺失会 502）。

# backend 上游（Fastify 平台 API）。Railway 服务名为 `stminiapp`，监听 8080。
BACKEND_UPSTREAM=stminiapp.railway.internal:8080   # [必填]

# ST 主体上游（SillyTavern，监听 8000）。prod 服务名 st-bundle-pro。
ST_UPSTREAM=st-bundle-pro.railway.internal:8000    # [必填]

# nginx 监听端口（容器内固定 80）。Railway 据此路由公网域名；不设会 502。
PORT=80                                            # [必填]
