export default function CreatePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md flex-col items-center justify-center pt-[env(safe-area-inset-top)]">
      <p className="text-[13px] text-muted-foreground">创作功能即将上线</p>
    </main>
  );
}
