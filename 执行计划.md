# 执行计划

> **⚠️ 历史性文档说明（D014 后追加）**：本文档是阶段一 D1-D12 的执行计划，反映 2026-05-28 之前规划时的 schema 状态（统一 `st` schema）。2026-06-03 的 D014 决策将 ST 同步层拆分为 `st_platform` / `st_users` / `st_infra` 三个 schema。本文档正文保留原貌作为历史记录，**最新 schema 架构与执行命令请查阅** [`packages/shared/migrations/README.md`](packages/shared/migrations/README.md) 与 [`DECISIONS.md`](DECISIONS.md) D014。文中"步骤 2：创建 st schema"等命令已被新版 `003_create_st_schemas.sql` 取代。

## 执行步骤

### 步骤 1：清理旧 migrations

目的：移除已过时的 001\-011 SQL 文件，为新 schema 腾出干净的起点

动作：

- 删除 `packages/shared/migrations/001\_\*\.sql` 到 `011\_\*\.sql` 共 11 个文件

- 更新 `packages/shared/migrations/README\.md` 为新索引

产出：干净的 migrations 目录

---

### 步骤 2：创建 `st` schema \+ 基础设施

目的：建立同步层独立 schema，与 `miniapp` 业务 schema 物理隔离

动作：编写 `001\_create\_st\_schema\.sql`

- `CREATE SCHEMA IF NOT EXISTS st`

- `REVOKE USAGE ON SCHEMA st FROM anon, authenticated`

- `GRANT USAGE ON SCHEMA st TO service\_role, postgres`

产出：`st` schema 就绪，anon/authenticated 不可访问

---

### 步骤 3：升级 `miniapp\.characters` 表

目的：为现有角色卡表补充同步管控字段

动作：编写 `002\_characters\_add\_sync\_fields\.sql`

- `ALTER TABLE miniapp\.characters ADD COLUMN is\_default BOOLEAN NOT NULL DEFAULT false`

- `ALTER TABLE miniapp\.characters ADD COLUMN enabled BOOLEAN NOT NULL DEFAULT true`

- `ALTER TABLE miniapp\.characters ADD COLUMN sort\_order INTEGER NOT NULL DEFAULT 0`

- 添加表注释标签 `\[partition=A\]\[shape=resource:platform\_pool\]\[direction=down\]`

产出：`miniapp\.characters` 具备 is_default / enabled / sort_order 管控能力

---

### 步骤 4：创建 `st\.platform\_settings` 表

目的：A 类配置型核心表，承载 settings\.json 全量快照 \+ 白名单定义

动作：编写 `003\_platform\_settings\.sql`

- 建表（id, platform_version, settings_jsonb, writable_paths, content_hash, created_at, created_by, note）

- `platform\_version` 单调递增 \+ UNIQUE

- 表注释标签 `\[partition=A\]\[shape=config\]\[direction=down\]`

产出：平台配置版本管理表就绪

---

### 步骤 5：创建 `st\.platform\_presets` 表

目的：A 类资产型，存储平台下发的 API 预设

动作：编写 `004\_platform\_presets\.sql`

- 建表（id, display_name, preset_payload JSONB, is_default, sort_order, enabled, created_at, updated_at）

- 表注释标签 `\[partition=A\]\[shape=resource:platform\_pool\]\[direction=down\]`

产出：预设池表就绪

---

### 步骤 6：创建 `st\.platform\_api\_configs` 表

目的：A 类资产型占位，阶段一存储平台 API Key

动作：编写 `005\_platform\_api\_configs\.sql`

- 建表（id, display_name, config_payload JSONB, is_default, created_at, updated_at）

- 表注释标签 `\[partition=A\]\[shape=resource:platform\_pool\]\[direction=down\]`

产出：API 配置表就绪（阶段一存 key \+ endpoint，保证调用路径可用）

---

### 步骤 7：创建 `st\.user\_st\_settings` 表

目的：B 类配置型，append\-only 记录用户白名单内的 settings 变更

动作：编写 `006\_user\_st\_settings\.sql`

- 建表（id, user_id FK, user_revision, settings_jsonb, based_on_platform_version, whitelist_version, content_hash, had_invalid_ref, source, audience, created_at）

- UNIQUE \(user_id, user_revision\)

- UNIQUE \(user_id, content_hash\) 幂等去重

- 表注释标签 `\[partition=B\]\[shape=config\]\[direction=up\]`

产出：用户设置镜像表就绪

---

### 步骤 8：创建 `st\.user\_st\_chats` 表

目的：B 类资产型占位，为阶段二聊天记录镜像留位

动作：编写 `007\_user\_st\_chats\.sql`

- 建表最小结构（id, user_id FK, character_id FK, chat_data JSONB, created_at）

- 表注释标签 `\[partition=B\]\[shape=resource:user\_pool\]\[direction=up\]`

产出：聊天记录表占位

---

### 步骤 9：RLS 策略

目的：统一锁定所有 st\.\* 表，service_role 唯一可访问

动作：编写 `008\_rls\_policies\.sql`

- 对 st\.\* 4 张表 \+ miniapp\.characters 启用 RLS

- REVOKE ALL FROM anon, authenticated

- GRANT ALL TO service_role

产出：权限隔离到位

---

### 步骤 10：种子数据

目的：写入阶段一可用的初始平台数据

动作：编写 `009\_seed\_data\.sql`

- `miniapp\.characters`：3 张种子卡（第一张 `is\_default = true`），复用之前的确定性 UUID

- `st\.platform\_presets`：1 行默认预设

- `st\.platform\_api\_configs`：1 行（OpenRouter key）

- `st\.platform\_settings`：第一行，内容为：
  - `platform\_version = 1`

  - `settings\_jsonb`：从 ST default\-user/settings\.json 导出的全量快照

  - `writable\_paths`：

  ```JSON
  [
    { "path": "active_character", "transform": "character_ref" },
    { "path": "oai_settings.prompts", "transform": "passthrough" }
  ]
  ```

产出：Supabase 具备完整初始数据，可驱动同步引擎运行

---

### 步骤 11：更新文档

目的：记录新决策，保持文档与实现一致

动作：

- 更新 `DECISIONS\.md`：追加新决策（D011: 新 schema 方案替换旧方案、D012: append\-only 策略、D013: Storage 存完整 PNG）

- 更新 `packages/shared/migrations/README\.md`：新的迁移索引

- 更新 `Schema划分设计\.md`：在\&\#34;八、未来扩展路径预演\&\#34;后补充本轮确认的结论（白名单初始字段、角色卡 PNG 存储策略）

产出：文档对齐

###

两个注意点：

1. A\. 白名单路径 oai_settings\.prompts 的粒度，如果实现起来比较简单不复杂的话，最好精细到每条的 enabled 字段（主要用于验证，为未来开放精细化配置做准备），如果实现成本在现阶段较高，则设为oai_settings\.prompts（整个 prompts 数组可改）。

2. B\. 种子 settings_jsonb 的来源,同意你的做法。
